module BotPlutusInterface.Collateral (
  getInMemCollateral,
  setInMemCollateral,
  filterCollateral,
) where

import Prelude qualified as Hask

getInMemCollateral = Hask.undefined

setInMemCollateral = Hask.undefined

filterCollateral = Hask.undefined -- (?) maybe for that single filter function will be required
